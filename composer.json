{
    "name": "mouf/utils.log.filter-logger",
    "description": "This package contains a class that acts as a logger. It purpose is to apply a filter on logs and forward the logs to another logger that will take care of storing the log.",
    "keywords": ["filter log", "mouf"],
    "homepage": "https://github.com/thecodingmachine/utils.log.filter-logger",
    "type": "mouf-library",
    "license": "MIT",
    "authors": [
        {
            "name": "David Négrier",
            "email": "d.negrier@thecodingmachine.com",
            "homepage": "http://mouf-php.com"
        }
    ],
    "require": {
    	"php": ">=5.3.0",
    	"mouf/mouf-installer": ">=2.0.0-dev",
        "mouf/utils.log.log-interface": "~2.0"
    },
    "autoload": {
        "psr-0": {
            "Mouf": "src/"
        }
    },
    "minimum-stability": "dev",
    "extra": {
	     "mouf": {
	     	"install": [
	    		{
	    			"type": "file",
	    			"file": "src/install.php"
	    		}
    		]
		}
    }
}
